{
	"name": "obsidian-quick-export",
	"version": "1.0.1",
	"description": "Right-click an Obsidian note or selection to save a copy outside the vault as .md or .txt.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && npm run check && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "biome check .",
		"check": "vitest run --passWithNoTests",
		"format": "biome check --write .",
		"lint:obsidian": "eslint ."
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"export",
		"markdown"
	],
	"author": "mikagosz",
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "^2.5.7",
		"@types/node": "^22.15.17",
		"esbuild": "0.25.5",
		"eslint": "^9.39.4",
		"eslint-plugin-obsidianmd": "^0.4.0",
		"globals": "^17.6.0",
		"jiti": "^2.6.1",
		"obsidian": "^1.13.1",
		"typescript": "^5.8.3",
		"vitest": "^4.1.10"
	},
	"engines": {
		"node": ">=20"
	}
}
